node_modules/
.expo/
dist/
web-build/

# Native projects — regenerated by `expo prebuild` / `expo run:*`
/ios
/android

# Model weights (6.4 MB binary) — fetch with `npm run get-model`
assets/*.tflite

# Secrets & local config
.env
.env.local
credentials.json
*.p8
*.p12
*.mobileprovision
*.keystore
!debug.keystore

# macOS / editors
.DS_Store
*.log
.idea/
.vscode/
